---
"@uiid/themes": patch
---

Create @uiid/themes package with schema, generator, validation, color utilities, and presets. Add theme preset selector to docs app with Server Action for on-the-fly CSS generation.
