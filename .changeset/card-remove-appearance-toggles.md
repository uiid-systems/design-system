---
"@uiid/cards": patch
---

Remove Card appearance toggles (`trimmed`, `inverted`, `transparent`, `ghost`) in favor of layout/style primitives (`p`, `b`) and consumer-owned styling. Alert no longer inherits these props.
