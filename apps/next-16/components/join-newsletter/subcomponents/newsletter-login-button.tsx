import { Button } from "@uiid/buttons";

export const NewsletterLoginButton = () => {
  return (
    <Button ghost size="small" fullwidth>
      Already a member? Log in.
    </Button>
  );
};
NewsletterLoginButton.displayName = "NewsletterLoginButton";
