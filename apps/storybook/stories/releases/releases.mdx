import { Meta } from "@storybook/addon-docs/blocks";
import { Changelog } from "./Changelog";

import designSystem from "../../../../packages/design-system/CHANGELOG.md?raw";

<Meta title="Releases" />

{/* To surface another active package's release notes:
    1. Import its CHANGELOG.md with Vite's `?raw` query.
    2. Render `<Changelog source={importedString} />` below.
    "Active" = published and not listed in .changeset/config.json `ignore[]`. */}

Release notes for [`@uiid/design-system`](https://github.com/uiid-systems/design-system).
Entries are authored as [changesets](https://github.com/changesets/changesets) on
the PRs that ship them.

<Changelog source={designSystem} />
