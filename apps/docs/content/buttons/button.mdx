---
component: Button
---

### Quick tips

- Polymorphic `render` prop can be used to compose a button with a custom trigger.
- Compose with an anchor or a custom `Link` component by setting the `nativeButton` prop to `false`.

<Preview />

---

### Properties

<PropsTable />
