/** Maximum number of colors supported in the gradient */
export const MAX_COLORS = 8;

/** Default gradient colors (pink to purple) */
export const DEFAULT_GRADIENT_COLORS = ["#FF9FFC", "#5227FF"];

/** Default rotation angle in degrees */
export const DEFAULT_ANGLE = 0;

/** Default noise intensity */
export const DEFAULT_NOISE = 0.3;

/** Default number of blinds */
export const DEFAULT_BLIND_COUNT = 16;

/** Default minimum width of each blind in pixels */
export const DEFAULT_BLIND_MIN_WIDTH = 60;

/** Default mouse movement dampening */
export const DEFAULT_MOUSE_DAMPENING = 0.15;

/** Default spotlight radius */
export const DEFAULT_SPOTLIGHT_RADIUS = 0.5;

/** Default spotlight softness */
export const DEFAULT_SPOTLIGHT_SOFTNESS = 1;

/** Default spotlight opacity */
export const DEFAULT_SPOTLIGHT_OPACITY = 1;

/** Default distortion amount */
export const DEFAULT_DISTORT_AMOUNT = 0;

/** Default shine direction */
export const DEFAULT_SHINE_DIRECTION = "left" as const;

/** Default mix blend mode */
export const DEFAULT_MIX_BLEND_MODE = "lighten" as const;
